# Contributing

- mantenha as mudanças focadas em fluxo, IA ou integração com Telegram
- rode `python -m py_compile *.py` antes de enviar mudanças de código
- não comite `.env`, `config.py`, `.venv` ou arquivos temporários
- atualize `README.md` se o comportamento do bot mudar
